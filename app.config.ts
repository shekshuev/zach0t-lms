export default defineAppConfig({
  ui: {
    card: {
      defaultVariants: {
        variant: "subtle",
      },
    },
    input: {
      defaultVariants: {
        variant: "soft",
      },
    },
    button: {
      defaultVariants: {
        variant: "soft",
      },
    },
  },
});
